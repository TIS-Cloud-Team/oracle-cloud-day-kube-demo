---
kind: Service
apiVersion: v1
metadata:
  name: http-echo
  namespace: default
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: http-echo
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
